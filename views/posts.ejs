<%- include('layout', { 
  title: 'All Blog Posts',
  body: `
    <h1>All Blog Posts</h1>
    <ul>
      ${posts.map(post => `
        <li>
          <a href="/posts/${post._id}">${post.title}</a>
          by ${post.author}
        </li>
      `).join('')}
    </ul>
  `
}) %>
